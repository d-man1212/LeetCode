Python:

from itertools import combinations

class Solution(object):
    def threeSum(self, nums):
        result = set(tuple(sorted(i)) for i in combinations(nums, 3) if sum(i) == 0)
        return [list(item) for item in result]
